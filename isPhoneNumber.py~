#!/usr/bin/env python

def isPhoneNumber(text):
    if len(text) != 12:
        return False 
    for i in range(0, 3):
        if not text[i].isdecimal():
            return False 

    if text[3] != '-':
        return False
    for i in range(4, 7):
        if not text[i].isdecimal():
            return False

    if cond:
        return 
    for var in collection:
        if cond:
            return 
    return 
            
    print()
    print()
    print()
    print()
        
        
    
def main():
    isPhoneNumber(text)

if __name__ == '__main__':
    main()
